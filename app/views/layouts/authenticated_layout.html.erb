<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= yield(:title) %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= csrf_meta_tags %>
  <%= yield(:extra_css) %>
      </head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

<body>
<nav class="navbar navbar-expand-md navbar-light fixed-top navbar-bg">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MABS</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Dashboard</a>
        </li>
        <!-- TODO: make this only show if user is admin -->
        <li class="nav-item">
          <%= link_to "Events", events_path, class: "nav-link"%>
        </li>
        <li class="nav-item">
          <%= link_to "Announcements", announcements_path, class: "nav-link"%>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/admin-tools">Admin Tools</a>
        </li>
        <li class="nav-item">
          <%= link_to "Check In", attendances_path, class: "nav-link"%>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
            <!-- Display sign-out button if the user is signed in -->
            <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
          <% else %>
            <!-- Display sign-in button if the user is not signed in -->
            <%= link_to 'Sign In', new_user_session_path %>
          <% end %>
        </li>
        <!-- Add more navigation items here -->
      </ul>
      <a>
        <% if user_signed_in? && @current_user.present? %>
          <div class="d-flex align-items-center justify-content-center bg-secondary" style="width: 40px; height: 40px; border-radius: 50%; overflow: hidden;">
            <%= image_tag @current_user.avatar_url, alt: "User Avatar", style: 'width: 100%; height: auto;' if @current_user.avatar_url.present? %>
          </div>
        <% end %>

      </a>

    </div>
  </div></nav>

      <%= yield %>

  <%= yield(:extra_js) %>
  </body>
</html>


